-- ══════════════════════════════════════════════
--  MediRecord Database Schema (NaCl / TweetNaCl)
--  NOTE: The `users` table has been REMOVED.
--  User identity is now stored on the Smart Contract
--  (see MedicalRecordSystem.sol → registerUser / getUser).
-- ══════════════════════════════════════════════

-- ─── encrypted_keys ─────────────────────────
CREATE TABLE IF NOT EXISTS encrypted_keys (
  id                         INT AUTO_INCREMENT PRIMARY KEY,
  cid                        VARCHAR(255) NOT NULL,
  user_address               VARCHAR(255) NOT NULL,
  encrypted_aes_key          TEXT         NOT NULL,
  nonce                      VARCHAR(64)  DEFAULT NULL,
  sender_address             VARCHAR(255) DEFAULT NULL,
  created_at                 TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY unique_cid_user (cid, user_address)
);


-- ─── access_requests ────────────────────────
CREATE TABLE IF NOT EXISTS access_requests (
  id              INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  patient_address VARCHAR(255) NOT NULL,
  doctor_address  VARCHAR(255) NOT NULL,
  operation       VARCHAR(100) NOT NULL,
  purpose         VARCHAR(255) NOT NULL,
  status          ENUM('pending','approved','rejected') DEFAULT 'pending',
  created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
